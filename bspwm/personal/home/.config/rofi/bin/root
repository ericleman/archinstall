#!/usr/bin/env bash

terminal=" Alacritty"
files=" Ranger"


if [ x"$@" = x"$terminal" ]
then
	coproc ( sudo -A alacritty  > /dev/null  2>&1 )
	exit 0
fi
if [ x"$@" = x"$files" ]
then
	coproc ( sudo -A alacritty -e ranger  > /dev/null  2>&1 )
	exit 0
fi


echo $terminal
echo $files
